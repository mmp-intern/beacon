spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/beacon-db?useUnicode=true&characterEncoding=utf8mb4&useSSL=false
    username: mmp
    password: qwerasdf
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect


# 디버깅 로그 설정
logging:
  level:
    org:
      springframework:
        security: DEBUG

# JWT 관련 설정
jwt:
  secret: your_very_long_secret_key_that_is_at_least_256_bits_long  # JWT 비밀 키
  expiration: 86400000  # JWT 만료 시간 (밀리초 기준, 여기서는 1일)